<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dc.galos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Authorization">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Menu" />
        <activity android:name=".View.Game" />
        <activity android:name=".View.Registration" />
        <activity android:name=".View.RestorePassword" />
        <activity android:name=".View.Profile" />
        <activity android:name=".View.Achievements" />
        <activity android:name=".View.Help" />
        <activity android:name=".View.Rating"></activity>
    </application>

</manifest>